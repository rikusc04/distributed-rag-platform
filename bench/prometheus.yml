global:
  scrape_interval: 2s
  evaluation_interval: 2s

scrape_configs:
  - job_name: mcp-gateway
    static_configs:
      # host.docker.internal → the gateway running on the mac host, :9090
      - targets: ["host.docker.internal:9090"]
